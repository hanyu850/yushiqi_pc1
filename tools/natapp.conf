# natapp 配置文件
# 将此文件放在 natapp.exe 同目录下

# authtoken 是你的隧道密钥
authtoken=3d394aaec0bfe11d

# 日志输出级别
log=none

# 日志文件
loglevel=DEBUG

# HTTP 监听端口（前端）
# 将本地 3000 端口映射到公网
# 访问 http://yushiqi.nat100.top 将转发到本地 http://127.0.0.1:3000

# 如果需要同时映射后端，请使用两条隧道或配置多个端口
# 后端端口 3001 可以通过另一个隧道映射
